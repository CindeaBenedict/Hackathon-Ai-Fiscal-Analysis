{
  "name": "supply-chain-command-center",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install --include=dev && VITE_API_BASE_URL= npm run build"
  },
  "engines": {
    "node": "20.x"
  }
}
